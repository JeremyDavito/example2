<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Backoffice Eshop-Project</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('app_backoffice_home') }}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_product_browse') }}">Products</a>
				</li>
				<li class="nav-item">
					<a class="nav-link"  href="{{ path('app_category_browse') }}" href="#">Categories</a>
				</li>
				<li class="nav-item">
					<a class="nav-link"  href="{{ path('app_delivery_index') }}" href="#">Delivery</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_order_index') }}">Orders</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_message_index') }}">Messages</a>
				</li>
				{% if is_granted('ROLE_MODERATOR') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_user_browse') }}">Users</a>
					</li>
				{% endif %}
			</ul>
		
		</div>
	</div>
</nav>
